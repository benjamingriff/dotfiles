# Literal replacements for `git filter-repo --replace-text`.
# Each line is `old literal==>new literal`.
# Keep the real file untracked as `.git-history-scrub`.

old sensitive literal==>***REMOVED***
